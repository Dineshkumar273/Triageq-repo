services:
  backend:
    build:
      context: ./backend
    env_file:
      - .env
    expose:
      - "5000"
    restart: unless-stopped

  web:
    build:
      context: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped
